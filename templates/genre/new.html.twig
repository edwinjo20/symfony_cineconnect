{% extends 'base.html.twig' %}

{% block title %}New Genre{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="card shadow-lg p-4 rounded-lg bg-dark text-light">
            <div class="card-body text-center">
                <h1 class="mb-4">Create New Genre</h1>

                {{ include('genre/_form.html.twig') }}

                <a href="{{ path('app_genre_index') }}" class="btn btn-secondary mt-3">
                    <i class="fas fa-arrow-left"></i> Back to List
                </a>
            </div>
        </div>
    </div>
{% endblock %}
